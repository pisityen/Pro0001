<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Print QR</title>
  <style>
    body {
      font-family: sans-serif;
      margin: 20px;
    }
    .sticker {
      width: 300px;
      height: auto;
      border: 1px solid #000;
      padding: 10px;
    }
    .qr {
      float: left;
      width: 100px;
    }
    .info {
      float: left;
      width: 180px;
      font-size: 14px;
      padding-left: 10px;
    }
    .pp {
      margin: 5px;
      margin-left: 15px;
    }
  </style>
</head>
<body>
  
  <div class="sticker">
    <p class="pp"><%= asset.as_name %></p>
    <div class="qr">
      <img src="<%= qrDataURL %>" alt="QR Code" width="100">
    </div>
    <div class="info">
      <p class=""><strong>Asset:</strong> <%= asset.as_asset_number %></p>
      <p class=""><strong>SN:</strong> <%= asset.as_serial_number || '-' %></p>
      <p class=""><strong>Date:</strong> <%= new Date(asset.created_at).toLocaleDateString('th-TH') %></p>
    </div>
    <div style="clear:both"></div>
  </div>
  <br><br>
  <button class="no-print" onclick="window.print()">🖨 พิมพ์สติ๊กเกอร์</button>
</body>
</html>
