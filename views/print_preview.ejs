<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Print QR Codes</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        body {
            background-color: #f0f2f5;
        }
        .sticker-sheet {
            background-color: white;
            padding: 1cm;
            width: 210mm; /* A4 width */
            min-height: 297mm; /* A4 height */
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            margin: 2rem auto;
        }
        .sticker-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr); /* 3 columns */
            gap: 10px;
        }
        .sticker {
            border: 1px dashed #ccc;
            padding: 10px;
            text-align: center;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            page-break-inside: avoid; /* Prevent breaking inside a sticker when printing */
        }
        .sticker img {
            max-width: 80%;
            height: auto;
        }
        .sticker .asset-number {
            font-family: monospace;
            font-weight: bold;
            font-size: 14px;
            margin-top: 5px;
            word-break: break-all;
        }
        .print-controls {
            position: fixed;
            top: 20px;
            right: 20px;
        }

        @media print {
            body { background-color: white; }
            .print-controls, .container > p { display: none; }
            .sticker-sheet { margin: 0; padding: 0.5cm; box-shadow: none; border: none; }
            .sticker { border: 1px solid #000; }
        }
    </style>
</head>
<body>
    <div class="container text-center py-4">
        <div class="print-controls">
            <button class="btn btn-primary btn-lg" onclick="window.print()"><i class="fas fa-print me-2"></i>พิมพ์</button>
            <button class="btn btn-secondary btn-lg" onclick="window.history.back()">กลับ</button>
        </div>
        <p class="lead">ตรวจสอบตัวอย่างก่อนพิมพ์ (แนะนำให้ตั้งค่าการพิมพ์เป็น "ขนาดจริง" หรือ "100%")</p>
        
        <div class="sticker-sheet">
            <div class="sticker-grid">
                <% if (assets && assets.length > 0) { %>
                    <% assets.forEach(asset => { %>
                        <div class="sticker">
                            <img src="<%= asset.qrDataURL %>" alt="QR Code for <%= asset.as_asset_number %>">
                            <div class="asset-number"><%= asset.as_asset_number %></div>
                            <small class="text-muted"><%= asset.as_name %></small>
                        </div>
                    <% }) %>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>
